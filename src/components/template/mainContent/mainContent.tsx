import React from "react";
import * as Styled from "./mainContent.style";

const MainContent: React.FC = () => {
  return <Styled.MainContent> Main </Styled.MainContent>;
};

export default MainContent;
